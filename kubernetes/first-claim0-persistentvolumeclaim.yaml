apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: first-claim0
  name: first-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
